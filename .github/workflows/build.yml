name: Build

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compose-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show Docker Compose version
        run: docker compose version

      - name: Build services from compose file
        run: docker compose build